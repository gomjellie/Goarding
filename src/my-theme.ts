import * as StyledComponent from 'styled-components/native';

// import ThemeInterface from "./src";

const {
  default: styled,
  css,
  ThemeProvider,
} = styledComponents as StyledComponent.ReactNativeThemedStyledComponentsModule<ThemeInterface>;
